To run this, you need to install [Flask](https://flask.palletsprojects.com/en/2.0.x/). Then, run the following command (in the folder where `web.py` is located):

    python web.py

Then, you should connect to http://127.0.0.1:5000/