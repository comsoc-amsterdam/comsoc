[uwsgi]
master = true
socket = :49162
smart-attach-daemon = /tmp/celery.pid celery -A wsgi.celery worker --pidfile=/tmp/celery.pid
processes = 8
enable-threads = true
wsgi-file = wsgi.py
callable = flask_app
